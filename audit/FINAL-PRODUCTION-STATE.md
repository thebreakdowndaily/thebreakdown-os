# FINAL PRODUCTION STATE — The Breakdown Knowledge Platform

Status: Live Handoff Record
Date: 31 Aug 2026
Governance: AGENTS.md v1.0 — Version Freeze / Founding Publication / CLDs no. 0016
Branch: `main` — pushed to `origin/main`, in sync, working tree clean.

---

## 1. Release Identity

| Field | Value |
|-------|-------|
| Repository HEAD | `93b10414a254f68a303e8c1f609afbe4bd10078a` |
| `origin/main` | `93b10414a254f68a303e8c1f609afbe4bd10078a` (in sync) |
| Branch state | `## main...origin/main` — clean, no pending commits |
| Push action | `git push origin main` — non-forced, `81c7118..93b1041 main -> main` |
| Commits pushed | 21 (the 20 TASK-23→30 stabilization commits + 1 release fix) |
| TASK coverage on main | TASK-01..30 implemented and merged |
| External integrations | NOT VERIFIED LIVE (see §6) |

---

## 2. Verification Run (on `main` @ 93b1041)

| Gate | Command | Result |
|------|---------|--------|
| Type check | `npm run check:type` (`tsc --noEmit`) | PASS — 0 errors |
| Lint (repo gate) | `npm run lint` | PASS on all TASK-24–TASK-30 files; 46 pre-existing errors remain in non-TASK files (see §5) |
| Full test chain | `npm test` (20-stage tsx chain) | PASS — all stages 0 failed (Retention 70, Learning 53, Auth 26/26, Content Refresh 11, Explorer 13, SEO 6, Homepage 11, Story 7, Entity 6, Distribution 6, etc.) |
| Production readiness | `npx tsx tests/production-readiness.test.ts` | PASS — 10/10 |
| Analytics taxonomy | `npx tsx tests/analytics-taxonomy.test.ts` | PASS — 33/33 |
| Citations | `npm run test:citations` (plugin) | PASS — engine resolution + KXE state transitions |
| Security | `npx tsx tests/security-certification.test.ts` | PASS — 13/13 |
| Trust metrics | `npx tsx tests/trust-metrics.test.ts` | PASS — 7/7 |
| Vitest suite (configured) | `npx vitest run` (security, security-certification, evidence-evolution, chapter-rendering, telemetry, canonical-certification, audit plugins) | PASS — 64/64 |
| Build | `npm run build` (`next build`) | PASS — route table emitted; shared JS 227 kB; 253 pages |
| Registry file | `tsconfig.tsbuildinfo` | Generated by `incremental` builds; reverted before push; tracked per existing policy (see §5) |

---

## 3. Release Fix Applied (final commit)

**Commit:** `93b1041` — `fix(analytics): rename membership_purchased param planId to snake_case plan_id`

- Found by `tests/analytics-taxonomy.test.ts`, which is NOT part of the `npm test` chain and therefore had slipped all gated runs.
- Real regression: `captureEvent('membership_purchased', { planId })` violated the analytics taxonomy contract (lowercase_snake_case params).
- Minimal changes, public API untouched:
  - `lib/analytics/capture.ts` line 92 — allow-list key `'membership_purchased': ['planId']` → `['plan_id']`
  - `app/membership/success/page.tsx` line 24 — `{ planId }` → `{ plan_id: planId }`
- Checkout flow URL/API `planId` contract intentionally unchanged (asserted by `tests/monetization/monetization.test.ts`, which still passes).
- Rerun after fix: analytics taxonomy 33/33; TypeScript clean.

---

## 4. Live Smoke Test — https://thebreakdown.in

Checked 31 Aug 2026 after push. Server: `cloudflare`; HSTS `max-age=63072000`; CSP present.

| Route | Status |
|-------|--------|
| `/` | 200 |
| `/robots.txt` | 200 |
| `/sitemap.xml` | 200 (112 URLs listed) |
| `/stories` | 200 |
| `/search?q=partition` | 200 |
| `/newsletter` | 200 |
| `/subscribe` | 200 |
| `/methodology` | 200 |
| `/trust` | 200 |
| `/founding-edition` | 200 |
| `/entities` / `/topics` | 200 |
| `/story/mgnrega-reform` | 200 — canonical + title correct |
| `/entity/un` | 200 |
| `/topic/economy` | 200 |
| `/series/foundations-1947-1962/volume/the-nehruvian-era/chapter/indias-inheritance` | 200 |
| `/api/health` | 401 (auth-gated; no 5xx) |
| `/api/checkout` POST | 401 (auth/rate-limit-gated; no 5xx) |

Security header observations:
- CSP active on all routes (default-src 'self'; frame-ancestors 'none' — clickjacking covered via CSP).
- HSTS active (2-year). NOTE: live value `63072000` is stronger than the `31536000` recorded in `audit/task-30/05-security-baseline.md` — documentation updated field below (baseline reflects middleware claim, not observed reality).
- `X-Content-Type-Options` is set on `/api/health` only, not globally; `X-Frame-Options` absent (redundant while `frame-ancestors 'none'` is active); `Referrer-Policy` absent. Candidate for a future Level A patch, not a release blocker.

---

## 5. Documentation-Explicit Observations (do not reclassify)

1. **Lint debt (pre-existing):** 46 errors in non-TASK files — `components/story/StoryShell.tsx` (16), `app/topic/[slug]/page.tsx` (20), untracked `services/repositories/memory/citation.ts` (10). Additionally `app/membership/success/page.tsx` carries 3 pre-existing errors (`no-unnecessary-condition` x2, `react-hooks/set-state-in-effect` x1) that predate the §3 fix. None were introduced by the release work; none are release blockers but all should be queued.
2. **Orphaned suites:** `tests/security-certification.test.ts` (13 tests) and `tests/trust-metrics.test.ts` (7 tests) pass under `tsx` but are NOT in the vitest `include` list in `vitest.config.js` and NOT in the `npm test` chain — they run in no automated gate. Recommend adding both to a gate.
3. **`tsconfig.tsbuildinfo` policy:** tracked in git and previously committed with feature work; `.gitignore` does not exclude it. Decision: leave tracking as-is; it is build-generated noise reverted before each push. Reconsider only via an explicit repository-wide convention change.
4. **Audit-directory naming gap:** `audit/task-01`, `audit/task-26`, `audit/task-27` have no directories, although TASK-26 and TASK-27 are implemented and merged on main (commits `b499097`, `17dd8be`). Documentation artifact only.
5. **Founding Edition status:** Chapter 1 (`/series/foundations-1947-1962/volume/the-nehruvian-era/chapter/indias-inheritance`) is live and serves the flagship knowledge object. Methodology, Constitution, Trust dashboard, Founding Edition page, and `/robots.txt` + `/sitemap.xml` are all live.

---

## 6. Integration Status — VERIFIED vs NOT VERIFIED

### Verified live
- HTTPS + HSTS + CSP (via `next.config.js` headers for all routes)
- SSR rendering of all route families (home, stories, search, methodology, trust, founding-edition, story, entity, topic, chapter)
- Canonical links, page titles, robots, sitemap
- Auth-gated API behavior (health, checkout) with no 5xx

### Verified in code (tests) but NOT verified against live providers
- **NOT VERIFIED LIVE — Google Analytics 4:** production events never observed in a live GA4 property. Code paths verified only.
- **NOT VERIFIED LIVE — Google Search Console:** live search-performance data unavailable. Sitemap/robots verified, indexing state unknown.
- **NOT VERIFIED LIVE — Stripe:** checkout redirects/wrappers complete; actual payouts and production key configuration pending.
- **NOT VERIFIED LIVE — Beehiiv:** subscribe/newsletter mechanics coded and tested (incl. live 422 and network-failure mapping in tests); live subscriber ingestion delivery not confirmed.
- **NOT VERIFIED LIVE — AdSense:** ad slot script wrappers ready; production configuration pending.
- **NOT VERIFIED LIVE — production numeric metrics:** no LCP/CWV/build-measured production numbers; nothing to quote. The release report's "metrics pending provider integration" statement holds.

Source of truth for these: `audit/task-30/16-FINAL-RELEASE-REPORT.md` (CONDITIONAL GO) and `05-security-baseline.md`.

---

## 7. Remaining Blockers to Public Launch (from TASK-30, unchanged)

1. Configure production env bindings for GA4, GSC, Stripe, Beehiiv, AdSense.
2. Live-prove those integrations end-to-end; re-run §2 gates once live data flows.
3. Recompute numeric production metrics once telemetry is live.
4. (Optional) Enforce RLS on DB migrations — noted Low in `13-release-scorecard.csv`.

## 8. Post-Launch Monitoring Priorities

1. Observe GA4 once bound: reader journey funnels (Reader starts → Evidence → Narrative → Complete → Continue Learning).
2. Watch `/api/checkout` + `/api/newsletter` rate-limit logs after public traffic begins.
3. Track Institutional Trust Index inputs on `/trust` (claims, sources, evidence, review completion).
4. Re-run `tests/analytics-taxonomy.test.ts` in the gated chain or wire it into CI so the §3 class of regressions cannot recur.
5. Review lint debt (§5.1) in a dedicated cleanup workstream matching the platform-beta rules (reader-visible or maintainability-bound).

---

Last verified: 31 Aug 2026, commit `93b10414a254f68a303e8c1f609afbe4bd10078a` on `main`, origin in sync.