/**
 * THE BREAKDOWN — Animations
 *
 * Public API: motion tokens, keyframes, transition helpers, stagger utilities.
 */

export * from './keyframes';
export { classes, stagger, transition } from './utilities';
