/**
 * THE BREAKDOWN — Colors
 *
 * Public API:
 *   palette   — raw color hex values
 *   semantic  — theme-aware semantic mappings (light + dark)
 */

export { palette } from './palette';
export { semantic } from './semantic';
export type { ThemeMode } from './semantic';
