import { BaseRegistry } from './BaseRegistry';

/**
 * Standard unified Registry implementation.
 * Use this directly rather than extending BaseRegistry.
 */
export class Registry<T> extends BaseRegistry<T> {}
